#!/usr/bin/env node

// Port numbers
var appPort = process.env.PORT || 5005;
var apiPort = process.env.API_PORT || 5006;

// Start App & API
require('../app')(appPort, apiPort);
require('../api')(apiPort);
